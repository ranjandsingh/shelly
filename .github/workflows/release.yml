name: Release

on:
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:
    inputs:
      force:
        description: "Re-release current version: move tag to main HEAD and rebuild (draft releases only)"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  create-tag:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      should_build: ${{ steps.reconcile.outputs.should_build }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from config
        id: version
        run: |
          VERSION=$(grep -oP '"version":\s*"\K[^"]+' src-tauri/tauri.conf.json | head -1)
          SHA=$(git rev-parse HEAD)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=v$VERSION" >> $GITHUB_OUTPUT
          echo "sha=$SHA" >> $GITHUB_OUTPUT
          echo "Releasing version: $VERSION at $SHA"

      - name: Reconcile tag and draft release
        id: reconcile
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE: ${{ inputs.force }}
          EVENT: ${{ github.event_name }}
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          SHA="${{ steps.version.outputs.sha }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          REMOTE_SHA=$(git ls-remote --tags origin "refs/tags/$TAG" | awk '{print $1}' || true)

          ensure_draft_release() {
            local tag="$1"
            local state
            state=$(gh release view "$tag" --repo "$GITHUB_REPOSITORY" --json isDraft --jq '.isDraft' 2>/dev/null || echo "missing")
            if [ "$state" = "missing" ]; then
              echo "Creating draft release $tag."
              gh release create "$tag" --repo "$GITHUB_REPOSITORY" --draft \
                --title "Shelly $tag" \
                --notes "See the assets below to download and install this version. macOS builds are currently unsigned — right-click and select Open to bypass Gatekeeper."
            else
              echo "Draft release $tag already exists."
            fi
          }

          if [ -z "$REMOTE_SHA" ]; then
            echo "Tag $TAG does not exist — creating at $SHA."
            git tag -a "$TAG" -m "Release $TAG" "$SHA"
            git push origin "$TAG"
            ensure_draft_release "$TAG"
            echo "should_build=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ "$REMOTE_SHA" = "$SHA" ]; then
            echo "Tag $TAG already points to $SHA — reusing."
            ensure_draft_release "$TAG"
            echo "should_build=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Tag exists on a different commit.
          if [ "$EVENT" != "workflow_dispatch" ] || [ "$FORCE" != "true" ]; then
            echo "Tag $TAG exists at $REMOTE_SHA but HEAD is $SHA."
            echo "Version was not bumped — skipping release build."
            echo "To re-release this version, run the workflow manually with force=true."
            echo "should_build=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Force path: move the tag, but refuse if the release was already published.
          STATE=$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json isDraft --jq '.isDraft' 2>/dev/null || echo "missing")
          if [ "$STATE" = "false" ]; then
            echo "Release $TAG is already published — refusing to move the tag."
            echo "Bump the version in src-tauri/tauri.conf.json instead."
            exit 1
          fi

          if [ "$STATE" = "missing" ]; then
            echo "Creating new draft release $TAG."
            gh release create "$TAG" --repo "$GITHUB_REPOSITORY" --draft \
              --title "Shelly $TAG" \
              --notes "See the assets below to download and install this version. macOS builds are currently unsigned — right-click and select Open to bypass Gatekeeper."
          else
            echo "Draft release $TAG already exists."
          fi

          echo "Force-moving tag $TAG from $REMOTE_SHA to $SHA."
          git tag -fa "$TAG" -m "Release $TAG" "$SHA"
          git push --force origin "$TAG"
          echo "should_build=true" >> $GITHUB_OUTPUT

  build:
    needs: create-tag
    if: needs.create-tag.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ''
          - platform: macos-latest
            args: '--target universal-apple-darwin'
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.create-tag.outputs.tag }}

      - uses: oven-sh/setup-bun@v2

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Updater signing — required to produce .sig artifacts
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # macOS signing
          # APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          # APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          # APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          # APPLE_ID: ${{ secrets.APPLE_ID }}
          # APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          # APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          # Point tauri-action at the pre-created draft release so it doesn't race
          # to create its own. We handle all asset uploads ourselves in the next step
          # because tauri-action@v0 silently skips .sig files on Tauri v2 builds.
          tagName: ${{ needs.create-tag.outputs.tag }}
          releaseDraft: true
          releaseBody: "See the assets below to download and install this version. macOS builds are currently unsigned — right-click and select Open to bypass Gatekeeper."
          args: ${{ matrix.args }}

      - name: Upload release assets
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ needs.create-tag.outputs.tag }}"

          # Collect all bundle artifacts into a temp file first.
          # This avoids the pipe-subshell problem where failures inside
          # `find ... | while read` are silently swallowed by bash.
          assets_file=$(mktemp)
          find src-tauri/target \
            -type f \
            \( \
              -name "*.exe" \
              -o -name "*.msi" \
              -o -name "*.dmg" \
              -o -name "*.tar.gz" \
              -o -name "*.zip" \
              -o -name "*.sig" \
            \) \
            | grep "bundle/" > "$assets_file" || true

          if [ ! -s "$assets_file" ]; then
            echo "::error::No bundle artifacts found under src-tauri/target. The Tauri build may have failed."
            exit 1
          fi

          # Fail fast here rather than letting the publish job discover the gap.
          sig_count=$(grep -c '\.sig$' "$assets_file" || true)
          if [ "$sig_count" -eq 0 ]; then
            echo "::error::No .sig files found in bundle artifacts. Ensure TAURI_SIGNING_PRIVATE_KEY is configured."
            exit 1
          fi

          total=$(wc -l < "$assets_file" | tr -d ' ')
          echo "Uploading $total assets ($sig_count .sig files) to $TAG..."

          # Read from the file (not a pipe) so this while loop runs in the current
          # shell — any gh failure is caught by set -e and aborts the step.
          while IFS= read -r asset; do
            echo "  -> $(basename "$asset")"
            gh release upload "$TAG" "$asset" --repo "${{ github.repository }}" --clobber
          done < "$assets_file"

  publish:
    needs: [create-tag, build]
    if: needs.create-tag.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Compose and upload latest.json
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ needs.create-tag.outputs.tag }}"
          VERSION="${{ needs.create-tag.outputs.version }}"
          REPO="${{ github.repository }}"
          BASE="https://github.com/$REPO/releases/download/$TAG"

          if ! gh release download "$TAG" --repo "$REPO" --pattern '*.sig' --dir sigs 2>/dev/null; then
            echo "::error::No .sig files found. Cannot generate latest.json. Ensure TAURI_SIGNING_PRIVATE_KEY is set."
            exit 1
          fi

          WIN_SIG=$(ls sigs/*_x64-setup.nsis.zip.sig 2>/dev/null | head -n 1 || true)
          if [ -z "$WIN_SIG" ]; then
            WIN_SIG=$(ls sigs/*_x64-setup.exe.sig 2>/dev/null | head -n 1 || true)
          fi
          MAC_SIG=$(ls sigs/*tar.gz.sig 2>/dev/null | head -n 1 || true)

          if [ -z "$WIN_SIG" ] || [ -z "$MAC_SIG" ]; then
            echo "::error::Missing expected .sig files. Found: $(ls sigs/ 2>/dev/null || echo 'none'). Cannot generate latest.json."
            exit 1
          fi

          # Extract base names without .sig for the URLs
          WIN_ASSET=$(basename "${WIN_SIG%.sig}")
          MAC_ASSET=$(basename "${MAC_SIG%.sig}")

          jq -n \
            --arg version "$VERSION" \
            --arg notes "See the release page for details." \
            --arg pub_date "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            --arg win_sig "$(cat "$WIN_SIG")" \
            --arg win_url "$BASE/$WIN_ASSET" \
            --arg mac_sig "$(cat "$MAC_SIG")" \
            --arg mac_url "$BASE/$MAC_ASSET" \
            '{
              version: $version,
              notes: $notes,
              pub_date: $pub_date,
              platforms: {
                "windows-x86_64": { signature: $win_sig, url: $win_url },
                "darwin-x86_64":  { signature: $mac_sig, url: $mac_url },
                "darwin-aarch64": { signature: $mac_sig, url: $mac_url }
              }
            }' > latest.json

          gh release upload "$TAG" latest.json --repo "$REPO" --clobber

      - name: Publish release
        run: |
          gh release edit "${{ needs.create-tag.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --draft=false
